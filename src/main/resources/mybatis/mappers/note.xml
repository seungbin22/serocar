<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.kosta.serocar.dao.NoteDAO">
  
  <insert id="insertNote">
  <![CDATA[
  	INSERT INTO NOTE
  	(note_no, note_to, note_from, note_content, memberemail)
  	VALUES(seq_note_no.NEXTVAL, #{note_to}, #{note_from,}, #{note_content}, #{memberEmail})
  	]]>
  </insert>
  
  <select id="myRecord" resultType="Note">
  <![CDATA[
  	SELECT * FROM NOTE
  	WHERE note_to = #{note_to}
  	]]>
  </select>
  
  </mapper>